name: Remote Trigger
on:
  repository_dispatch:
    types: [remote_start_test]
jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Signal Received
        run: echo "Test session triggered via web portal"
